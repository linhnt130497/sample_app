<% provide :title, t(".all_users") %>
<h1><%= t ".all_users" %></h1>

<ul class="users">
<% if @user.present? %>
    <%= render @user %>
  <% else %>
    <div class="alert alert-warning"><%= t "no_user" %></div>
  <% end %>
</ul>
